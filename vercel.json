{
    "version": 2,
    "builds": [
      {
        "src": "gulpfile.js",
        "use": "@vercel/static-build",
        "config": { "distDir": "prod" }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }